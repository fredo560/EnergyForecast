name: myenv
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pandas
  - numpy
  - requests
  - pytz
  - pip
  - pip:
      - gridstatus==0.30.1
      - gridstatusio>=0.14.0
      - yfinance
